import { getFakeDataHelper } from "../helpers/getFakeDataHelper/getFakeDataHelper";

/**
 * Initial state for the application
 */
export const INITIAL_STATE = {
    advisorsList: getFakeDataHelper()
}