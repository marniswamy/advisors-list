import { getFakeDataHelper } from '../getFakeDataHelper';

describe('getFakeDataHelper', () => {
  it('should ', () => {
    const expected = getFakeDataHelper();
    const result = true;
    expect(expected).toEqual(result);
  });
});
