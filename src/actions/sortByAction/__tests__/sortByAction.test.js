import { sortByAction } from '../sortByAction';

describe('sortByAction', () => {
  it('should ', () => {
    const expected = sortByAction();
    const result = true;
    expect(expected).toEqual(result);
  });
});
