import { filterByAction } from '../filterByAction';

describe('filterByAction', () => {
  it('should ', () => {
    const expected = filterByAction();
    const result = true;
    expect(expected).toEqual(result);
  });
});
